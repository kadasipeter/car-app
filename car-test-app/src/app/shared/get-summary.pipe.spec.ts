import { GetSummaryPipe } from './get-summary.pipe';

describe('GetSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
